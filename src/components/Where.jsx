function Where() {
    // Send a GET request to the MirageJS server /api/events

    return (
        <div id="where">
            <h1>Where section</h1>
        </div>
    );
}

export default Where;
